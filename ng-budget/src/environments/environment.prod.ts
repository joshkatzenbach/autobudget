export const environment = {
  production: true,
  // Update this URL to match your production API endpoint
  // For build-time replacement, use: ng build --configuration=production --define=API_URL="https://api.yourdomain.com/api"
  apiUrl: 'https://api.yourdomain.com/api'
};

