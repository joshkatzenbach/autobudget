[build]
builder = "NIXPACKS"
# Build command is handled by nixpacks.toml
# Railway will auto-detect Node.js version from .nvmrc or package.json engines

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
