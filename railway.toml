# This file is kept for reference, but Railway will use backend/railway.toml
# when the root directory is set to /backend in Railway dashboard
# 
# For Railpack configuration, see: backend/railway.toml
